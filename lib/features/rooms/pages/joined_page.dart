import 'package:flutter/material.dart';


class JoinedPage extends StatelessWidget {
  const JoinedPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
