

enum SnackBarType {
  error,
  success,
  unconnected,
  info,
}

enum PostStatus {
  feedPost,
  deletedPost,
  archivedPost,
}

enum ActionNotification {
  chatMessage,
  notification,
  startChat,
  feedback,
}

enum ActionCreatePost {
  gallery,
  camera,
  text,
}

enum SearchFrom {
  messagesScreen,
  homeScreen,
  createStoryScreen,
}

enum BookingState {
  waiting,
  accepted,
  canceled,
  completed,
}

enum LayoutMode {
  defaultLayout,
  full,
  hostTopCenter,
  hostCenter,
}

enum ActionSelect {
  gallery,
  camera,
}